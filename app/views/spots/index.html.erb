<div class="container mt-5 mb-5">
  <div class ="row">
    <div class="col-md-12 text-center mx-auto">
      <h2>
        それでは、この中から心惹かれる写真を選んでみましょう。<br>
        それがあなたの目的地です。
      </h2>
    </div>
  </div>
</div>

<div class="container">
  <div class ="row">
      <div class="col-md-12 display">
        <% @spot.each do |spot| %>
          <%= form_with model: spot, url: similar_spot_path, method: :GET, local:true do |f| %>
            <!--simiralアクションに選択肢の情報を送る-->
            <%= f.hidden_field :select,  :value => spot.id %>
            <%= f.hidden_field :choice1, :value => params[:choice1] %>
            <%= f.hidden_field :choice2, :value => params[:choice2] %>
            <!--/simiralアクションに選択肢の情報を送る-->
            <%= f.button :type => "submit", style: "border-style: none; background-color: transparent;" do %>
              <div class="mb-5"><%= attachment_image_tag spot, :image, class: "image-middle",  fallback: "no_image.png" %></div>
            <% end %>
          <% end %>
        <% end %>
      </div>
  </div>
</div>